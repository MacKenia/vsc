#include<stdio.h>

int main()
{
    int n, i;
    char c[3][3];
    scanf("%d", &n);
    for (i = 0; i < n; i++)
    {
        gets(c[i]);
    }
    return 0;
}

int f(char *p)
{
    int i, j, k;
    for(; *p != '\n'; p++)
    {
        if(*p == 'X');
    }
    return 0;
}

