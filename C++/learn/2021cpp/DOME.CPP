//34_5的答案
#include <bits/stdc++.h>
#define MAX_NUMBER numeric_limits<int>::max

using namespace std;

int main()
{
    ofstream inputfile;
    inputfile.open("C:/users/MacKenia/Desktop/a.txt",ios::out);
    if(inputfile) ;
    else("打开失败");
    inputfile.close();
    return 0;
}
