#include<iostream>
#include<vector>
using namespace std;

int main()
{
    vector<int> v[24];
    int n;
    char b, d;
    string a ,c;
    scanf("%d", &n);
    for(int i=0; i < n; i++) v[0].push_back(i);
    while(cin >> a >> b >> c >> d&&a !='quit')
    {

    }
    return 0;
}

